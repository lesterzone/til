When working with `PS1` we can display [several options](https://zsh.sourceforge.io/Doc/Release/Prompt-Expansion.html#Prompt-Expansion).

My personal preference with `zsh` is:

```sh
%2~ $
```

It will display two level folders like:

```
develop/nodejs $
```

So I know where I am but keep it clean